salvare immagine profilo
quando viene eliminato un utente, eliminare anche la foto dalla cartella
--------------------------------------
schema validator per messages
schema validator per users

style messaggi